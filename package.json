{
  "name": "my-app",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bunx tsc",
    "start": "bun run dist/index.js",
    "seed": "bun src/infraestructure/database/seed.ts",
    "prisma:generate": "prisma generate"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.2",
    "@types/bcrypt": "^5.0.2",
    "bcrypt": "^5.1.1",
    "hono": "^4.6.16",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}